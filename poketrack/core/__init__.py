"""Core data layer: models, regions, database, parser, scheduler, service.

This package is intentionally free of any UI imports so it can be reused by the
desktop GUI, the web GUI, tests, or a future CLI without modification.
"""
