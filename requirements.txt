# PokéTrack dependencies
customtkinter>=5.2.2     # Local dark-mode desktop GUI
Pillow>=10.0.0          # Event thumbnails in the desktop UI (CTkImage)
Flask>=3.0.0            # Web GUI
APScheduler>=3.10.4     # Background event updates
requests>=2.31.0        # HTTP client (webhooks + image cache)
httpx>=0.27.0           # Async HTTP client for non-blocking event fetches
beautifulsoup4>=4.12.0  # HTML fallback parser (official blog source)
python-dateutil>=2.9.0  # Robust date parsing helpers
plyer>=2.1.0            # Optional desktop notifications (gracefully skipped if unavailable)
pystray>=0.19.0        # Optional system-tray icon for the desktop app (optional)
